<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>品牌線上月刊圖書館 | ✨ AI 智慧管理版</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .glass-morphism {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .magazine-card:hover img {
            transform: translateY(-10px);
            box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
        }
        #ai-chat-window.hidden { display: none; }
        .typing-dot {
            width: 6px;
            height: 6px;
            background: #4f46e5;
            border-radius: 50%;
            animation: typing 1s infinite shadow;
        }
        @keyframes typing {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-4px); }
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-900 font-sans">

    <!-- 導覽列 -->
    <nav class="sticky top-0 z-50 glass-morphism shadow-sm">
        <div class="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
            <div class="flex items-center space-x-2">
                <div class="w-10 h-10 bg-indigo-600 rounded-lg flex items-center justify-center text-white shadow-lg">
                    <i class="fas fa-book-open"></i>
                </div>
                <span class="text-xl font-bold tracking-tight">品牌數位月刊 <span class="text-xs bg-indigo-100 text-indigo-600 px-2 py-0.5 rounded ml-1">AI 管理版</span></span>
            </div>
            <div class="hidden md:flex space-x-8 text-sm font-medium">
                <a href="#" class="hover:text-indigo-600 transition">官方網站</a>
                <a href="#" class="hover:text-indigo-600 transition">線上商城</a>
                <button onclick="toggleAIChat()" class="text-indigo-600 hover:text-indigo-700 font-bold flex items-center">
                   <i class="fas fa-sparkles mr-1"></i> ✨ AI 助理
                </button>
            </div>
            <a href="#" class="bg-indigo-600 text-white px-5 py-2 rounded-full text-sm font-semibold hover:bg-indigo-700 transition shadow-md">
                前往購物 <i class="fas fa-shopping-cart ml-1"></i>
            </a>
        </div>
    </nav>

    <!-- 主視覺：最新一期展示 -->
    <section id="latest-section" class="py-12 bg-white border-b relative overflow-hidden">
        <!-- 這裡會由 JavaScript 自動填入最新一期內容 -->
    </section>

    <!-- 歷期月刊列表 -->
    <section id="library" class="py-16 max-w-7xl mx-auto px-4">
        <div class="flex flex-col md:flex-row md:items-end justify-between mb-10 gap-6">
            <div>
                <h2 class="text-3xl font-bold text-slate-900">歷期月刊回顧</h2>
                <p class="text-slate-500 mt-2">共 <span id="total-count">0</span> 期精彩內容</p>
            </div>
            <div class="flex flex-wrap items-center gap-4">
                <select id="year-filter" onchange="renderMagazines()" class="bg-white border border-slate-200 rounded-lg px-4 py-2 text-sm focus:ring-2 focus:ring-indigo-500 outline-none shadow-sm">
                    <option value="all">所有年份</option>
                    <option value="2026">2026 年</option>
                    <option value="2025">2025 年</option>
                </select>
            </div>
        </div>

        <!-- 月刊網格：由 JavaScript 生成 -->
        <div id="magazine-grid" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-8"></div>
    </section>

    <!-- AI 聊天視窗 (保持原樣) -->
    <div id="ai-chat-window" class="fixed bottom-6 right-6 w-96 h-[500px] glass-morphism shadow-2xl rounded-2xl flex flex-col z-[100] hidden border-indigo-200">
        <!-- ... (聊天室內容同上版) ... -->
    </div>

    <!-- AI 懸浮按鈕 -->
    <button onclick="toggleAIChat()" class="fixed bottom-6 right-6 w-16 h-16 bg-indigo-600 text-white rounded-full shadow-2xl flex items-center justify-center hover:scale-110 transition z-50 group">
        <i class="fas fa-wand-magic-sparkles text-2xl"></i>
    </button>

    <script>
        const apiKey = ""; 

        // --- 📒 這是你唯一需要「編輯」的地方 ---
        // 當你有第 150 期時，只需要在最前面新增一筆資料
        const magazines = [
            { id: "149", title: "春季生活提案", date: "2026.03", year: "2026", cover: "https://via.placeholder.com/300x400?text=Issue+149", heyzineUrl: "https://heyzine.com/flip-book/your-id-149" },
            { id: "148", title: "冬季暖心選物", date: "2026.02", year: "2026", cover: "https://via.placeholder.com/300x400?text=Issue+148", heyzineUrl: "https://heyzine.com/flip-book/your-id-148" },
            { id: "147", title: "新年煥新氣象", date: "2026.01", year: "2026", cover: "https://via.placeholder.com/300x400?text=Issue+147", heyzineUrl: "https://heyzine.com/flip-book/your-id-147" },
            { id: "146", title: "聖誕特別企劃", date: "2025.12", year: "2025", cover: "https://via.placeholder.com/300x400?text=Issue+146", heyzineUrl: "https://heyzine.com/flip-book/your-id-146" },
            // ... 你可以一次把 149 期的資料補在這裡
        ];

        // 初始化網頁
        window.onload = () => {
            renderLatest();
            renderMagazines();
            document.getElementById('total-count').innerText = magazines.length;
        };

        // 渲染最新一期 (主視覺)
        function renderLatest() {
            const latest = magazines[0];
            const container = document.getElementById('latest-section');
            container.innerHTML = `
                <div class="max-w-7xl mx-auto px-4">
                    <div class="grid md:grid-cols-2 gap-12 items-center">
                        <div>
                            <span class="bg-indigo-100 text-indigo-700 text-xs font-bold px-3 py-1 rounded-full">LATEST ISSUE</span>
                            <h1 class="text-4xl md:text-5xl font-extrabold mt-4 leading-tight">第 ${latest.id} 期：<br><span class="text-indigo-600">${latest.title}</span></h1>
                            <p class="mt-6 text-slate-600 text-lg leading-relaxed">發行日期：${latest.date}</p>
                            <div class="mt-8 flex flex-wrap gap-4">
                                <button onclick="scrollToLibrary()" class="bg-slate-900 text-white px-8 py-3 rounded-xl hover:bg-slate-800 transition shadow-lg">瀏覽往期</button>
                                <button onclick="askGemini('摘要第${latest.id}期內容')" class="border-2 border-indigo-600 text-indigo-600 px-8 py-3 rounded-xl font-bold hover:bg-indigo-50 transition">✨ AI 導讀</button>
                            </div>
                        </div>
                        <div class="aspect-[4/3] bg-slate-200 rounded-2xl overflow-hidden shadow-2xl border-4 border-white">
                            <iframe src="${latest.heyzineUrl}" class="w-full h-full" frameborder="0" allowfullscreen></iframe>
                        </div>
                    </div>
                </div>
            `;
        }

        // 渲染歷期列表 (網格)
        function renderMagazines() {
            const filterYear = document.getElementById('year-filter').value;
            const grid = document.getElementById('magazine-grid');
            grid.innerHTML = '';

            const filtered = magazines.filter(m => filterYear === 'all' || m.year === filterYear);

            filtered.forEach(m => {
                grid.innerHTML += `
                    <div class="magazine-card group cursor-pointer" onclick="window.open('${m.heyzineUrl}', '_blank')">
                        <div class="aspect-[3/4] rounded-xl overflow-hidden mb-4 relative transition duration-300">
                            <img src="${m.cover}" alt="${m.title}" class="w-full h-full object-cover shadow-sm">
                            <div class="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition flex items-center justify-center">
                                <span class="bg-white text-slate-900 px-4 py-2 rounded-full text-xs font-bold">點擊閱讀</span>
                            </div>
                        </div>
                        <h3 class="font-bold text-slate-800 group-hover:text-indigo-600 transition">第 ${m.id} 期</h3>
                        <p class="text-xs text-slate-400 font-medium mt-1">${m.date}</p>
                    </div>
                `;
            });
        }

        function toggleAIChat() { document.getElementById('ai-chat-window').classList.toggle('hidden'); }
        function scrollToLibrary() { document.getElementById('library').scrollIntoView({ behavior: 'smooth' }); }
        
        // --- Gemini AI 邏輯 (同前版) ---
        async function askGemini(query) {
            // ... (同上版程式碼) ...
        }
    </script>
</body>
</html>